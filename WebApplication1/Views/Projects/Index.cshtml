@model IEnumerable<WebApplication1.Models.Project>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PLocation)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Department.DName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PLocation)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Department.DName)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.PNumber }) |
            @Html.ActionLink("Details", "Details", new { id=item.PNumber }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.PNumber })
        </td>
    </tr>
}

</table>
